const rootUrl = 'http://localhost:8000/';
const loginUrl = `${rootUrl}rest-auth/login/`
const userParamsUrl = `${rootUrl}user/`
const workUrl = `${rootUrl}works/`
const workIdUrl = `${rootUrl}worksid/`

export {
    loginUrl,
    userParamsUrl,
    workUrl,
    workIdUrl,
    rootUrl,

}